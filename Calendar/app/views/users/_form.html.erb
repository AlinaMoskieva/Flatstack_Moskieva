<% @user = current_user %>
<%= form_for(current_user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>

  <br>
  <div class="actions" align="center">
    <%= f.submit "UPDATE USER", class: " small secondary button" %>
     <%= link_to 'return', user_path(@user.id), class: " small secondary button" %>
  </div>
<% end %>






